/**** Start of imports. If edited, may not auto-convert in the playground. ****/
var imageCollection = ee.ImageCollection("projects/hardy-tenure-383607/assets/EUFO_Forest2020_v1_post_MMUAreaProj");
/***** End of imports. If edited, may not auto-convert in the playground. *****/
function deleteChildren(ImgCollection){
var n = ImgCollection.size().getInfo();
  var colList = ImgCollection.toList(n);
  var assetFolder = 'projects/hardy-tenure-383607/assets/EUFO_Forest2020_v1_post_MMUAreaProj/';
  
  for (var i = 0; i < n; i++) {
    var img = ee.Image(colList.get(i));
    var id = img.id().getInfo() || 'image_'+i.toString();
    var assetId = assetFolder+id;
    ee.data.deleteAsset(assetId);
  }
}


deleteChildren(imageCollection)