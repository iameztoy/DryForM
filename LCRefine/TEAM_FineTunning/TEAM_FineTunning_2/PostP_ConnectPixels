/**** Start of imports. If edited, may not auto-convert in the playground. ****/
var mv = ee.ImageCollection("projects/hardy-tenure-383607/assets/MajorityVote_p_gt05"),
    vp_or = {"opacity":1,"bands":["Trees"],"max":1,"palette":["020401","4eff2f"]},
    vp_cp = {"opacity":1,"bands":["Trees"],"palette":["000000","ff0c0c"]},
    vp_mx = {"opacity":1,"bands":["Trees"],"max":1,"palette":["000000","64ac17"]},
    vp_final = {"opacity":1,"bands":["Trees"],"palette":["287d1e"]},
    geometry = 
    /* color: #ffc82d */
    /* displayProperties: [
      {
        "type": "rectangle"
      }
    ] */
    ee.Geometry.Polygon(
        [[[-57.293185215920055, -17.355081553989297],
          [-57.293185215920055, -17.426995997718336],
          [-57.19692609879359, -17.426995997718336],
          [-57.19692609879359, -17.355081553989297]]], null, false);
/***** End of imports. If edited, may not auto-convert in the playground. *****/

var m = ee.ImageCollection('projects/hardy-tenure-383607/assets/MajorityVote_p_gt05').mosaic();

var un = m.unmask();
var cp = un.connectedPixelCount(100).lte(50);
var cp_unm = cp.updateMask(cp);
var mix = un.add(cp);

var mix_sf = mix.selfMask();
var final = mix_sf.connectedPixelCount(100).gte(50).selfMask();


Map.addLayer(un, vp_or, "original unmasked");
Map.addLayer(mix, vp_mx, "Mix - corrected CP <= 50");
Map.addLayer(cp_unm, vp_cp, "Only Connected Pixes <= 50");
Map.addLayer(final, vp_final, "Final - CP => 50");
Map.addLayer(cp.selfMask(), vp_cp, "connected pixels lte 50");

Map.setOptions('SATELLITE');

var test = un.connectedPixelCount(100)
Map.addLayer(test, null, "test")

// Export the image

Export.image.toDrive({
  image: final,
  description: 'Postprocessed',
  //crs: projection.crs,
  //crsTransform: projection.transform,
  region: geometry
});

Export.image.toDrive({
  image: un,
  description: 'original',
  folder: "GEEtest",
  //crs: projection.crs,
  //crsTransform: projection.transform,
  region: geometry
});

Export.image.toDrive({
  image: cp,
  description: 'CP_lte50',
  folder: "GEEtest",
  //crs: projection.crs,
  //crsTransform: projection.transform,
  region: geometry
});