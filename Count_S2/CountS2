/**** Start of imports. If edited, may not auto-convert in the playground. ****/
var chaco = ee.FeatureCollection("users/iameztoy/dryform/AOIs/eco_zone_Chaco"),
    imageVisParam = {"opacity":1,"bands":["B8"],"max":8,"palette":["ff310c","ffc07b","fffd2e","c0ff65","0aff0d","5bfffd","2a81ff","f06dff"]},
    cerrado = ee.FeatureCollection("users/iameztoy/dryform/AOIs/eco_zone_Cerrado"),
    caatinga = ee.FeatureCollection("users/iameztoy/dryform/AOIs/eco_zone_Caatinga"),
    EstanciaEriana = /* color: #d63000 */ee.Geometry.Point([-61.326942237670885, -21.412422503967058]),
    ZonadeRioVerde = /* color: #50d62b */ee.Geometry.Point([-59.175143579885315, -23.19118088147772]);
/***** End of imports. If edited, may not auto-convert in the playground. *****/
var srt = '2020-10-01'; var end = '2020-11-01'; 
// Example Estancia Faro Moro S2 Intersect. 
// Count: 15, 18, 17, 20, 24, 14, 19, 18, 17, 18, 19, 16
var CLOUD_FILTER = 80; // 70 does not cover all

var AOI = ZonadeRioVerde
var AOI_all = chaco.merge(cerrado).merge(caatinga)
var AOI_clip = chaco

var s2 = (ee.ImageCollection('COPERNICUS/S2_SR')
        .filterBounds(AOI)
        .filterDate(srt, end)
        .filter(ee.Filter.lte('CLOUDY_PIXEL_PERCENTAGE', CLOUD_FILTER)));

print(s2)

var count = s2.count()
var select = count.select("B8")
print(select)

var count_clip = select.clip(AOI_clip)

var red = s2.min();
var bands = ['B8', 'B4', 'B3'];
Map.addLayer(red, {'bands': bands, 'min': 0, 'max': 2500}, "s2", 1)

Map.addLayer(select,imageVisParam, "Sentinel-2 Count x3 areas")
Map.addLayer(count_clip,imageVisParam, "Sentinel-2 Count in AOI")
Map.addLayer(AOI, null, "AOI")


 
 

